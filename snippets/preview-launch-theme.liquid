<style>
  .launch-preview {
    --lp-bg: #f3ede3;
    --lp-surface: rgba(255, 250, 244, 0.94);
    --lp-surface-strong: #fbf6ef;
    --lp-line: #d8c7b5;
    --lp-text: #231913;
    --lp-muted: #6b6057;
    --lp-accent: #5a3b2a;
    --lp-accent-strong: #3d281c;
    --lp-sage: #5f7568;
    --lp-sand: #eee2d2;
    --lp-shadow: 0 24px 60px rgba(58, 39, 25, 0.08);
    --lp-radius: 28px;
    --lp-radius-sm: 18px;
    --lp-max: 1180px;
    --lp-serif: "Iowan Old Style", "Palatino Linotype", "Book Antiqua", Georgia, serif;
    --lp-sans: "Avenir Next", "Segoe UI", "Helvetica Neue", sans-serif;
    background:
      radial-gradient(circle at top right, rgba(95, 117, 104, 0.12), transparent 28%),
      radial-gradient(circle at left 14%, rgba(166, 120, 86, 0.10), transparent 26%),
      linear-gradient(180deg, #f8f3ec 0%, var(--lp-bg) 100%);
    color: var(--lp-text);
    font-family: var(--lp-sans);
  }

  .launch-preview,
  .launch-preview *,
  .launch-preview *::before,
  .launch-preview *::after {
    box-sizing: border-box;
  }

  .launch-preview__container {
    width: min(var(--lp-max), calc(100% - 32px));
    margin: 0 auto;
  }

  .launch-preview__surface {
    background: var(--lp-surface);
    border: 1px solid var(--lp-line);
    border-radius: var(--lp-radius);
    box-shadow: var(--lp-shadow);
  }

  .launch-preview__surface--tight {
    border-radius: var(--lp-radius-sm);
  }

  .launch-preview__eyebrow {
    margin: 0 0 12px;
    color: var(--lp-sage);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .launch-preview__title {
    margin: 0;
    font-family: var(--lp-serif);
    font-weight: 500;
    line-height: 1.02;
    letter-spacing: -0.03em;
  }

  .launch-preview__title--xl {
    font-size: clamp(40px, 6vw, 78px);
  }

  .launch-preview__title--lg {
    font-size: clamp(34px, 5vw, 58px);
  }

  .launch-preview__title--md {
    font-size: clamp(28px, 3vw, 42px);
  }

  .launch-preview__lead {
    margin: 0;
    color: var(--lp-muted);
    font-size: 18px;
    line-height: 1.75;
  }

  .launch-preview__body,
  .launch-preview__body p,
  .launch-preview__body li {
    color: var(--lp-text);
    font-size: 16px;
    line-height: 1.75;
  }

  .launch-preview__body p,
  .launch-preview__body ul,
  .launch-preview__body ol {
    margin: 0 0 16px;
  }

  .launch-preview__body ul,
  .launch-preview__body ol {
    padding-left: 20px;
  }

  .launch-preview__muted {
    color: var(--lp-muted);
  }

  .launch-preview__chip-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .launch-preview__chip {
    display: inline-flex;
    align-items: center;
    min-height: 34px;
    padding: 0 14px;
    border: 1px solid var(--lp-line);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.7);
    color: var(--lp-accent-strong);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .launch-preview__chip--sage {
    background: rgba(95, 117, 104, 0.10);
    color: var(--lp-sage);
  }

  .launch-preview__chip--accent {
    border-color: var(--lp-accent);
    background: var(--lp-accent);
    color: #fff7ef;
  }

  .launch-preview__chip--sand {
    background: var(--lp-sand);
  }

  .launch-preview__button-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .launch-preview__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 48px;
    padding: 0 22px;
    border: 1px solid var(--lp-accent);
    border-radius: 999px;
    background: var(--lp-accent);
    color: #fff7ef;
    font-size: 15px;
    font-weight: 700;
    letter-spacing: 0.01em;
    text-decoration: none;
    transition: transform 120ms ease, background 120ms ease, color 120ms ease;
  }

  .launch-preview__button:hover {
    transform: translateY(-1px);
  }

  .launch-preview__button--secondary {
    background: transparent;
    color: var(--lp-accent);
  }

  .launch-preview__badge {
    position: absolute;
    top: 18px;
    left: 18px;
    z-index: 1;
    padding: 8px 12px;
    border-radius: 999px;
    background: rgba(35, 25, 19, 0.84);
    color: #fff7ef;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .launch-preview__media-frame {
    position: relative;
    overflow: hidden;
    border-radius: var(--lp-radius);
    background: linear-gradient(135deg, #efe2d1 0%, #faf5ef 100%);
  }

  .launch-preview__media-frame picture,
  .launch-preview__media-frame img {
    display: block;
    width: 100%;
    height: 100%;
  }

  .launch-preview__media-frame img {
    object-fit: cover;
  }

  .launch-preview__placeholder {
    display: flex;
    min-height: 100%;
    align-items: center;
    justify-content: center;
    padding: 32px;
    text-align: center;
    color: var(--lp-muted);
  }

  .launch-preview__fineprint {
    margin: 0;
    color: var(--lp-muted);
    font-size: 13px;
    line-height: 1.75;
  }

  .launch-preview__table {
    width: 100%;
    border-collapse: collapse;
  }

  .launch-preview__table th,
  .launch-preview__table td {
    border-top: 1px solid var(--lp-line);
    padding: 16px 0;
    text-align: left;
    vertical-align: top;
    font-size: 15px;
    line-height: 1.65;
  }

  .launch-preview__table tr:first-child th,
  .launch-preview__table tr:first-child td {
    border-top: 0;
    padding-top: 0;
  }

  .launch-preview__table th {
    width: 32%;
    padding-right: 18px;
    font-weight: 600;
  }

  .launch-preview__faq details {
    padding: 18px 0;
    border-top: 1px solid var(--lp-line);
  }

  .launch-preview__faq details:first-child {
    padding-top: 0;
    border-top: 0;
  }

  .launch-preview__faq summary {
    cursor: pointer;
    list-style: none;
    font-size: 16px;
    font-weight: 700;
    line-height: 1.55;
  }

  .launch-preview__faq summary::-webkit-details-marker {
    display: none;
  }

  .launch-preview__faq summary + p {
    margin-top: 12px;
  }

  .launch-preview__form-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .launch-preview__input {
    flex: 1 1 260px;
    min-width: 0;
    min-height: 48px;
    padding: 0 16px;
    border: 1px solid var(--lp-line);
    border-radius: 999px;
    background: #ffffff;
    color: var(--lp-text);
    font-size: 15px;
  }

  .launch-preview__card-grid {
    display: grid;
    gap: 20px;
  }

  .launch-preview__card {
    padding: 24px;
    border: 1px solid var(--lp-line);
    border-radius: var(--lp-radius-sm);
    background: var(--lp-surface-strong);
  }

  .launch-preview__card h2,
  .launch-preview__card h3 {
    margin: 0 0 10px;
    font-family: var(--lp-serif);
    line-height: 1.15;
    letter-spacing: -0.02em;
  }

  .launch-preview__card h2 {
    font-size: clamp(24px, 2.8vw, 34px);
  }

  .launch-preview__card h3 {
    font-size: 24px;
  }

  .launch-preview a {
    color: var(--lp-accent);
  }

  @media screen and (max-width: 749px) {
    .launch-preview__container {
      width: min(var(--lp-max), calc(100% - 24px));
    }

    .launch-preview__surface {
      border-radius: 22px;
    }

    .launch-preview__title--xl {
      font-size: 42px;
    }

    .launch-preview__title--lg {
      font-size: 36px;
    }

    .launch-preview__title--md {
      font-size: 30px;
    }

    .launch-preview__lead {
      font-size: 17px;
    }

    .launch-preview__table th,
    .launch-preview__table td {
      display: block;
      width: 100%;
      padding-right: 0;
    }

    .launch-preview__table td {
      padding-top: 0;
      padding-bottom: 16px;
    }
  }
</style>
